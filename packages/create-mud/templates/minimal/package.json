{
  "private": true,
  "name": "{{kebab name}}",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "prepare": "pnpm workspace contracts prepare",
    "dev": "run-pty % pnpm dev:node % pnpm dev:client % pnpm dev:contracts",
    "dev:client": "pnpm workspace client run dev",
    "dev:node": "pnpm workspace contracts run devnode",
    "dev:contracts": "pnpm workspace contracts run dev"
  },
  "devDependencies": {
    "run-pty": "^4.0.3"
  }
}
